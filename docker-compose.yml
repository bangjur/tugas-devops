version: '3'
services:
  web:
    build: .
    ports:
      - "5000:5000"  # Format is "host_port:container_port"
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=production  # Changed to production for security
    restart: always  # Added for reliability